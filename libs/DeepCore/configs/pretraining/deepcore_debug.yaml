defaults:
  - optimizer: sgd_deepcore
  - scheduler: cosine_annealing_lr
batch_size: 128
max_epochs: 1

seed: 0
str: batch_size=${.batch_size}-${.max_epochs}-${.optimizer.str}-scheduler=${.scheduler.str}-seed=${.seed}