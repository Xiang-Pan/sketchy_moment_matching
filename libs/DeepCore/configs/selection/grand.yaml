defaults:
  - dataset: cifar10
  - training: deepcore
  - backbone: resnet50

method: grand
fraction: 0.01
batch_size: 128
selection_batch: 128
workers: 4
seed: 0
device: cuda
print_freq: 20
num_classes: 10

str: ${.method}-${.fraction}-${.selection_batch}-${.seed}