method: cov
class_conditioned: False
data_type: eb
fraction: 0.001
seed: 0
c: 2
max_epochs: 100
sparse_scale: 10
optimization: sgd

s_eps: 0
s_lr: 1e-2
gamma_lr: 1e-2


# str: ${.method}-${.data_type}-${.final_fraction}-${.seed}-${.c}/ratio=${.fraction}
str: ${.method}-${.data_type}-${.seed}-${.c}-${.fraction}
str_without_fraction: ${.method}-${.data_type}-${.seed}-${.c}