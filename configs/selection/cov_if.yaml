defaults:
  - IF: LiSSA
method: influence_function

#! Note: we do not use that in the ratio selection
fraction: 0.4
rep: mlp_50

data_type: eb
seed: 0
str: method=${.method}-data_type=${.data_type}-seed=${.seed}-${.IF.str}