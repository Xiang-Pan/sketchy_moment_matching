method: random
data_type: eb
fraction: 0.001
seed: 0

# str: ${.method}-${.data_type}-${.final_fraction}-${.seed}-${.c}/ratio=${.fraction}
str: ${.method}-${.data_type}-${.seed}-${.fraction}
str_without_fraction: ${.method}-${.data_type}-${.seed}