defaults:
  - optimizer: null
  - scheduler: null
seed: 0
name: cifar10
rep: null
arch: mlp_50
hidden_layer_sizes: 
  - 50
train_batch_size: -1
val_batch_size: -1
test_batch_size: -1
sample_weight: null

# str: ${.name}-${.seed}-${.sample_weight}
str: name=${.name}-seed=${.seed}-sample_weight=${.sample_weight}-arch=${.arch}